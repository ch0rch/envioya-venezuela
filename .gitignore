node_modules/
dist/
.astro/
.env
.DS_Store

# Internal dev scaffolding — not published
docs/superpowers/
scripts/fetch-fixtures.sh
.superpowers/

# Build artifacts — must never be committed (breaks Vercel: it uses prebuilt output with local paths)
.vercel/
dist/
